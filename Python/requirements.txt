# --- Core Dependencies ---
pydantic>=2.0
pydantic-settings
python-dotenv
pyserial>=3.5

# --- GUI & Data ---
# Note: Tkinter must be installed via apt (python3-tk) on Linux/RPi
pillow>=10.0
pandas>=2.0

# --- Testing ---
pytest>=7.4.3
 pytest-mock
# coverage

# --- Documentation ---
# mkdocs-material

# --- Hardware (Raspberry Pi Only) ---
# Uncomment if running on RPi hardware
RPi.GPIO

# --- Async Core (Demeter V2) ---
textual
pyserial-asyncio
pydantic
customtkinter
aiosqlite
aiosqlite
