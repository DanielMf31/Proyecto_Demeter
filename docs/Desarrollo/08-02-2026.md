Plan de Desarrollo de hoy

1. ESP32: Completado, funciona
    1. Implementar la función de encender leds físicos en GPIOController
    2. Implementar en el main.py la inicialización de los pines
    3. Probar en Hardware real la función de control por comando en Monitor Serial
    4. Probar función simple de control de gpio según comando hardcodeado en main_receptor.cpp
2. Python
    1. Entender cómo funciona Tkinter y las interfaces en Python
    2. Simplificar el creador de protocolo para mandar por UART sin destino ni nada
    3. Probar que compila todo el sistema de Python con configuraciones hardcodeadas
    4. Pasar todos los tests y crear el entorno virtual
3. Raspberry
    1. Subir el código a Github y clonar el repo dentro de la Raspberry Pi
    2. Conectar la Raspberry Pi y enviar tramas mediante la interfaz gráfica
    3. Comprobar que el sistema funciona de forma autónomo y sin problemas
    4. Subir el código final a Python y planificar próximo movimiento


# 1. ESP32

## 1.1. Implementar la función de encender leds físicos en GPIOController

1. Pedirle a Antigravity que explique cómo implementaría la función simple.
    1. Leer Respuesta
    2. Si convence, pedirle que lo implemente
    
## 1.2. Implementar en el main.py la inicialización de los pines

1. Pedirle a Antigravity que explique cómo implementaría la función simple.
    1. Leer Respuesta
    2. Si convence, pedirle que lo implemente

## 1.3. Probar en Hardware real la función de control por comando en Monitor Serial

1. Subir código a ESP32 S3 mediante la interfaz de PlatformIO dentro de VSCode ya que es la forma más simple
2. Probar el control mediante Monitor Serial y los comandos ismples para comprobar si los LEDS se encienden
3. Si lo hacen, guardar código, implementar tests, pasar tests y terminar etapa. 

# 2. Python

## 2.1 Entender cómo funciona Tkinter y las interfaces en Python

1. Pedirle a Antigravity que explique cómo funciona Tkinter y las interfaces en Python
    1. Leer Respuesta
    2. Si convence, pedirle que lo implemente

PROBLEMA: No puedo ejecutar el main dentro del portátil, sólo para ver si compila. Subo el texto a Antigravity para ver qué sucede. 


