Okay vale creo que ya se me ha ocurrido como organizar mejor el código del firmware. Ahora mismo tenemos implmentada la activación de los ejecutores y gpio y el Protocolo en general, pero falla a la hora de la sincronización y activación de cosas. En este caso, creo que en SystemContext que quiero cambiar a SystemManager en este caso es donde vamos a implementar los distitnos tipo de acciones del sistema. En este caso primero quiero aprovechar para definir todos los callbacks de ProtcolEngine y qué hacer ante cada uno. Primero quiero implementar un protocol de Handshake usando unos comandos SYNC, ACK que ya está y SYN-ACK para poder asegurar la conectividad tanto por UART como por ESP-Now. Después de eso existe el enviar Data que puede ser también una acción o función definida dentro de SystemManagaer, al igual que enviar cualquier de los Reportes en General. Lo que harías sería definir por ejemplo la etapa de Handshake, la etapa de envío de información o la de Comandos. De esta forma, luego simplemente en el Nodo lo que harías sería definir una función que fuera setup que lo que único que hiciera sería activar el Nodo y dejar que funcionara correctamente sólo. De esta forma, dentro del Nodo quizás lo que podemos hacer es implementar la interfaz de comandos por Serial, hacienod que al presionar un botón concreto se intente hacer un Handshake con el Nodo Actuador y si ocurre bien entonces ahí es cuando envías el comando, o la info y demás. De esta forma SystemManager se encargaría de todo lo relacionado con el protocolo, la gestión de estados ya que existe el estado enviar_ack, esperar_ack y todas estas cosas, y nodo sólo se encargaría de la interfaz respecto al usuario. De esta forma en los main tan sólo sería cambiar el ID del nodo para cada uno y en Nodo en sí defniríamos los pines que todos los nodos comparten, como por ejemplo definir 16,17 como los pines de UART y en general hacerlos fijos para que siempre todos los Nodos tengan los mismo pines y se sepa fácilmente dónde colocar cada cosa. Genera en este caso un .md y guárdalo en Design con estos conceptos, dime qué patrones de diseño se usan para crear esto, analiza los archivos de firmware actuales y dime cómo implementarlo porfavor 
