Okay perfecto me gustaría que ahora analizaras y crearas un capítulo específico para la Raspberry en el que expliques con el mismo grado de detalle como funciona la Raspberry, como se conecta al endpoints ws, como funciona internamente con el dispatcher que es el adaptador entre json y los comandos, cómo funciona el módulo del Protocolo Demeter, el módulo de asincronía y demás, en general que se vea bien el funcionamiento igual que con los nodos del firmware. Resalta la naturaleza de usar docker para el código, el acceso a internet a través de cable Ethernet y demás. 

Así como también el acceso a los puertos locales, cómo envía la ifnormación y demás

Después de eso me gustaría que analizaras el Backend. En este caso sería otro capítulo específico. Dentro de este capítulo qiero que detalles la arquitectura del sistema dockerizado por compnentes, cad auno de los contenedores y para qué sirven, que existe en un servidor en la nube en una máquina virtual que actualmente es de Google Cloud, que se conecta a la base de datos de PostgreSQL, al caché de Redis, al worker, al secuenciador y demás. La relación interna, explica detaladamente la comunicación entre los distintos módulos de Docker, el módulo de caché con redis y para qué sirve y qué datos guarda, el modulo de PostgresSQL con las principales tablas y para qué sirve y qué datos guarda, el worker y para qué sirve y qué tareas realiza, el secuenciador y para qué sirve y qué tareas realiza, el frontend y para qué sirve y qué funcionalidades ofrece, el backend y para qué sirve y qué funcionalidades ofrece. También con un enlace al anexo donde está el diagrama de la base de datos, cómo se obtiene la información. En concreto detallada la documentación de la API, los principales endpoints, qué hace cada uno y demás 

Detalla muchísimo la API porfavor, este capítulo debe ser increíblemente detallado en general cada módulo. Haz primero un plan de Implementación de todos los temas primero