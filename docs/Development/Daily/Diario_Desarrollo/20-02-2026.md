1. Implementar generación de datos de gráficas. Prompt: 



1. Pedirle a la IA que me genere el Prompt.
2. Meterle a Antigravity el Prompt, más este prompt de IA más indicaciones para que lo haga para el entorno de pruebas e inyecte los datos y genere el servicio de autenticación. También preguntar cómo generar diagramas de bases de datos


3. Redis con pub/sub, caché y multiworker cambia algo la cosa o cómo se configura


1. Documentación Arquitectura (30 min)
    1. TODO: Preparar la Arquitecutra del proyecto según el modelo de 4 capas (Contexto, Contenedores, Componentes y Clases).
    2. PLAN:
        1. Pedirle a la IA que me genere los diagramas del modelo de 4 componentes según el proyecto actual.
        2. Compilarlo en imágenes usando el comando d2 y su CLI. Instalarlo primero
        3. Leer todos los diagramas, asegurar que son correctos y están bien documentados

    3. ANOTACIONES:
        1. El Diagrama de Contexto debe tener sólamente el usuario, servidor, Raspberry y Nodos de ESP32
        2. El Diagrama de Contenedores debe tener los contenedores de cada uno de los anteriores
        3. El Diagrama de Componentes debe tener los componentes de cada uno de los contenedores
        4. El Diagrama de Clases debe tener las clases de cada uno de los componentes
        5. 
    4. POSIBLES PROBLEMAS
        1. Los Diagramas no salen como he pedido. Analizar los falllos y pedirle que los genere otra vez.
        2. 
    5. DESARROLLO TAREA
        1. Voy a pedir que genere los diagramas.
            1. No me los ha hecho con d2 como quería.
            2. No me ha hecho exáctamente la base de datos en el formato que quería.
            3. Me acabo de dar cuenta que es buena idea generar un workflow para la documentación de arquitectura
            4. Me ha creado el diagrama de base de datos y el de contexto (Nivel 1) -> Voy a generar el diagrama de contenedores y pedirle que me ayude a generar un Workflow para la documentación de arquitectura
            5. Le he pedido a la IA que cambie y cree un formato más sobrio para la arquitectura para que sea más profesional. 
            6. Me ha generado los diagramas de componentes y contenedores del Servidor, Raspberry y los Nodos.
                1. En el Diagrama de los ESP32 debo dar contexto muy detallado de cómo funciona la arquitectura para que pueda hacer documentación bien
            7. Voy a darle contexto especializado para que entienda cómo funciona
            8. Okey ya he generado el diagrama de componentes y contenedores del Servidor, Raspberry y los Nodos. 
                1. Los diagramas de la ESP32 están incompletos o módulos que no existen. Los modificaré más adelante.
    6. OBJETIVOS COMPLETADOS
        1. Diagrama de Contexto del sistema
        2. Diagrama de Contenedores del Servidor
        3. Diagrama de Contenedores de la Raspberry
    7. OBJETIVOS PENDIENTES
        1. Diagrama de Contenedores de los Nodos ESP32
        2. Diagrama de Componentes de los Nodos ESP32
        3. Diagrama de Clases de los Nodos ESP32
2. Refinamiento Sistema de Cálculo de Variables agronómicas (1 hora)
    1. TODO: Refinar el sistema de cálculo con un script para generar gráficas y otro para generar cálculos
    2. PLAN:
        1. Mover el script de cálculos agronómicos de Playground a Analisis Matemático
        2. Generar un script para generar gráficas
        3. Generar un main para juntar ambas funcionalidades y usar redis queu para gestionar la ejecución de los cálculos
        4. Modificar el script de inyección de base de datos para que inyecte 24 horas de datos del día de ayer desde las 00:00 hasta las 23:00, 24 mediciones en total
        5. Levantar el sistema entero de nuevo ahora y probar a extraer los datos una vez pregunten y devolver el Excel y las gráficas.
    3. ANOTACIONES:
        1. Pedirle a la IA que me ayude a generar el script de gráficas y el main
        2. 
    4. POSIBLES PROBLEMAS
        1. 
    5. DESARROLLO TAREA
        1. Le voy a pedir a la IA que analice el código de Análisis datos ahora mismo y use el script que le he dado para calcular todos estos valores, eliminando los valores relacionados con el sol.
            1. Quiero que simplemente no use esos cálculos, pero que calcule todo el resto, y los guarde en los Excel
            2. Quiero que en base a esos datos genere los cálculos. ¿Se puede hacer con gather para aprovechar la asincronía o no?
            3. Reformular el sistema para guardarlos de forma permanente y almacenar metadatos para que si la persona vuelve a pedirlo no tenga que calcularse y se sirva otra vez el .zip. Guardar los archivos en el servidor.
            4. Okay ya funcionan las gráficas y el sistema de caché específico para que se guarden las gráficas y no tarde más al descargarse.
            5. Ahora le voy a pedir que se puedan ver las gráficas interactivas como antes en la web y seleccionar la planta y además añadir la gráfica de la VPD en la Web.
                1. Nada más entrar en la página we enviará por json a través de una nueva llamada de api del frontend al backend los datos de la base de datos crudos de humedad y temperatura y se calculará directamente en el frontend la VPD para mostrarla también. Se lo voy a pedir. El Backend enviará por defecto los datos en json de los últimos 30 días
    6. OBJETIVOS COMPLETADOS
        1. 
    7. OBJETIVOS PENDIENTES
        1. 

