Okay perfecto te explico. Ahora quier oen este caso implementar el tema es de un nodo actuador. Este nodo actuador actuaría controlando un motor que tendría en este caso un final de carrera por lo que sólo debemos preocupabarnos de hacer que se encienda o se apague un gpio que controla un contactor que es el que se encarga de activar la apertura de unas ventanas en lel Invernadero mediante un motor en el techo. El objetivo es definir este nodo actuador. Se encargaría a su vez de enviar en este caso varias variables como Estado Motor que se actualizaría en cada momento que se cambia ese gpio y además el estado de la batería que se mediría la batería incorporada que tiene para avisar de cuándo cambiar el sistema en este caso. Había pensado que cada vez que se actualiza el gpio se puede aprovechar y enviar también el estado de la batería así pudiendo aprovechar el DataReport o crea acaso otro tipo similar en este caso que fuera comandos específicos para activar GPIO como ya tenemos, ahora veeremos. En este caso lo que tendríamos sería un main_nodo_actuador_ventana.cpp que integraría toda la lógica que ya hemos creado por detrás. De esta forma, configuraríamos uno o dos GPIO que serían los que tendrían al final que cambiar de estado. En este caso se enviarían las órdenes y todo por ESP-Now desde el Gateway principal, se hardcodearía por el momento la MAC del ESP nuevo actuador para poder enviar. Básicamente habŕa que diseñar una serie de tests. En este caso debería comprobar que recibe y envía datos usando el protocolo, que se monitoriza la batería, el estado del movimiento, el control remoto, es posible de entrar a Light sleep y se enciende cuando recibe un ping (Mockear), y crear en este caso un mock simulado dentro de la Raspberry igual que acabamos de hacer en este caso con los sensores, sería hacer un --mock y poder elegir si queremos un mock de sensores o de actuador en este caso. También habría que añadir un botón específico para poder darle y cambiar el estado de ese motor dentro de la interfaz gráfica y linkearlo con el comportamiento interno, mandar la orden al gateway y que este la mande hacia afuera. El mock entero debería ser algo como 1. Enciendes el servidor con el modo mock, y simulas que hay un nodo-actuador escuchando. En este caso presionas el botón y se simula el enviar los datos en este caso de ping primero, y cuando recibe confirmación enviar el comando de activación, el esp responde con un comando específico que informa del estado del gpio y de la batería o de algún otro dato espna vez hecho esto se simula la recepción de esa información y se envía la confirmación y se después de haber desactivado otra vez el motor se informa de que se entrará en Light Sleep y se simula que luego el ESP se ha apagado. Con todo esto se consideraría la feature creada. Crea un PRD y documentación para poner toda esta información y asegúrate de que todo lo que he dicho en este mensaje está correcto en el PRD y luego empieza a implementarlo, crea tests y haz que pasen y de esta forma podemos asegurar que es rentable hacer esto. @SKILL.md @SKILL.md @SKILL.md @.agent/skills/python-best-practices @SKILL.md 