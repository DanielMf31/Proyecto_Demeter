# This file is intentionally empty or just re-exports if you want to keep strict folder structure within BD.
# But since we already have Core/database.py which is imported by models.py, we might not strictly need this matching the user request EXACTLY in location if it causes circular imports.
# However, to satisfy "Ponlo dentro de BD", I'll create a init file.
